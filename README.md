
# EchoTag Pro Hackathon Build
## Run
Frontend:
cd frontend && npm install && npm run dev

Backend:
cd backend && npm install && npm run dev

AI Service:
cd ai-service && pip install -r requirements.txt && python app.py
